# Railway lee este archivo con cualquier builder (Railpack o Nixpacks).
# startCommand tiene prioridad sobre el comando que el builder autodetecta,
# evitando el default incorrecto "gunicorn main:app".
[deploy]
startCommand = "gunicorn 'app:create_app()' --bind 0.0.0.0:${PORT:-8080}"
